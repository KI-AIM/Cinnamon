/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AnonymizationAttributeConfigurationDirectiveDirective } from './anonymization-attribute-configuration-directive.directive';

describe('Directive: AnonymizationAttributeConfigurationDirective', () => {
  it('should create an instance', () => {
    const directive = new AnonymizationAttributeConfigurationDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
