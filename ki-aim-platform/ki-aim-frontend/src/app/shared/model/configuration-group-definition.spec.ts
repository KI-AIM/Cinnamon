import { ConfigurationGroupDefinition } from './configuration-group-definition';

describe('ConfigurationGroupDefinition', () => {
  it('should create an instance', () => {
    expect(new ConfigurationGroupDefinition()).toBeTruthy();
  });
});
