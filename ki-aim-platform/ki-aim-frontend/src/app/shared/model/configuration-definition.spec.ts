import { ConfigurationDefinition } from './configuration-definition';

describe('ConfigurationDefinition', () => {
  it('should create an instance', () => {
    expect(new ConfigurationDefinition()).toBeTruthy();
  });
});
