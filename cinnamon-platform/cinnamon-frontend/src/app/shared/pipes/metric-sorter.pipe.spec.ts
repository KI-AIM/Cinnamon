import { MetricSorterPipe } from './metric-sorter.pipe';

describe('MetricSorterPipe', () => {
  it('create an instance', () => {
    const pipe = new MetricSorterPipe();
    expect(pipe).toBeTruthy();
  });
});
