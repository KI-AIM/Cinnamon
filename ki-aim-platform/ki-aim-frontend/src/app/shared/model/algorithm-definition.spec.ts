import { AlgorithmDefinition } from './algorithm-definition';

describe('AlgorithmDefinition', () => {
  it('should create an instance', () => {
    expect(new AlgorithmDefinition()).toBeTruthy();
  });
});
