package org.bihmi.jal.anon.util;

public class GeneralizationHelper {
    // finds orders of dates in given dataset
    // generalizes date columns to single given date column
    // creates generalization hierarchies for dates
    // transforms relative date intervals back to dates
}
