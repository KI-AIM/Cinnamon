<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  Deliberately narrow ruleset: catches likely bugs and dead code without
  fighting this codebase's existing conventions (tabs for indentation,
  wildcard imports are allowed). Run with `mvn checkstyle:check` per module,
  or `mvn checkstyle:check -pl <module> -am`.
-->
<module name="Checker">
    <module name="TreeWalker">
        <module name="UnusedImports"/>
        <module name="RedundantImport"/>
        <module name="EqualsHashCode"/>
        <module name="SimplifyBooleanExpression"/>
        <module name="SimplifyBooleanReturn"/>
        <module name="StringLiteralEquality"/>
        <module name="OneTopLevelClass"/>
        <module name="FallThrough"/>
        <module name="EmptyCatchBlock">
            <!-- allow the `catch (SomeException ignored) {}` idiom already used in this codebase -->
            <property name="exceptionVariableName" value="^ignored$"/>
        </module>
        <module name="EqualsAvoidNull"/>
    </module>
    <module name="NewlineAtEndOfFile">
        <property name="lineSeparator" value="lf_cr_crlf"/>
    </module>
</module>
