import { ColumnConfiguration } from "./column-configuration";

describe("ColumnConfiguration", () => {
	it("should create an instance", () => {
		expect(new ColumnConfiguration()).toBeTruthy();
	});
});
