import { MetricFilterPipe } from './metric-filter.pipe';

describe('MetricFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MetricFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
