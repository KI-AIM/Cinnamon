import { StageConfiguration } from './stage-configuration';

describe('StageConfiguration', () => {
  it('should create an instance', () => {
    expect(new StageConfiguration()).toBeTruthy();
  });
});
