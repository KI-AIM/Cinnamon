export enum ConfigurationInputType {
    ATTRIBUTE = 'attribute',
    ATTRIBUTE_LIST = 'attribute_list',
    BOOLEAN = 'boolean',
    FLOAT = 'float',
    INTEGER = 'integer',
    LIST = 'list',
    NAMED_LIST = 'named_list',
    STRING = 'string',
}
